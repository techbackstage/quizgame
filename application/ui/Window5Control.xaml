<UserControl x:Class="QuizGame.Application.UI.Window5Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             Background="#1E1B2E">

    <UserControl.Resources>
        <Style x:Key="AnswerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2C3C"/>
            <Setter Property="Foreground" Value="#E6E8F0"/>
            <Setter Property="BorderBrush" Value="#3D4054"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="auto"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#4B5AFF"/>
                    <Setter Property="Background" Value="#31344A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsRevealed}" Value="True"/>
                        <Condition Binding="{Binding IsCorrect}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#28A745"/>
                    <Setter Property="BorderBrush" Value="#218838"/>
                    <Setter Property="Foreground" Value="White"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsRevealed}" Value="True"/>
                        <Condition Binding="{Binding IsSelected}" Value="True"/>
                        <Condition Binding="{Binding IsCorrect}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#DC3545"/>
                    <Setter Property="BorderBrush" Value="#C82333"/>
                    <Setter Property="Foreground" Value="White"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid MaxWidth="800" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Grid>
                <TextBlock x:Name="QuestionProgressTextBlock" Text="Frage 1 / 10" FontSize="16" Foreground="#A6A8B8" HorizontalAlignment="Left"/>
                <TextBlock x:Name="ScoreTextBlock" Text="Punkte: 0" FontSize="16" Foreground="#A6A8B8" HorizontalAlignment="Center"/>
                <TextBlock x:Name="TimerTextBlock" Text="00:20" FontSize="20" FontWeight="Bold" Foreground="#4B5AFF" HorizontalAlignment="Right"/>
            </Grid>
            <ProgressBar x:Name="TimeProgressBar" Height="8" Margin="0,8,0,0" Background="#2A2C3C" BorderThickness="0" Foreground="#4B5AFF" Value="100"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock x:Name="QuestionTextBlock" Text="Lade Frage..." TextWrapping="Wrap" FontSize="28" FontWeight="SemiBold" Foreground="#E6E8F0" Margin="0,0,0,32" TextAlignment="Center"/>
            <ItemsControl x:Name="AnswersItemsControl" ItemsSource="{Binding CurrentAnswers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.AnswerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource AnswerButtonStyle}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Border x:Name="ExplanationPanel" Grid.Row="2" Background="#2A2C3C" CornerRadius="8" Padding="20" Margin="0,20,0,0" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="Erklärung" FontSize="18" FontWeight="Bold" Foreground="#4B5AFF" Margin="0,0,0,8"/>
                <TextBlock x:Name="ExplanationTextBlock" Text="" TextWrapping="Wrap" Foreground="#A6A8B8" Margin="0,0,0,16"/>
                <Button x:Name="NextQuestionButton" Content="Nächste Frage" Height="48" FontSize="18" FontWeight="SemiBold" Background="#4B5AFF" Foreground="#E6E8F0" BorderThickness="0" Click="NextQuestionButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
